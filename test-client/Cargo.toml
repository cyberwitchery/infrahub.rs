[package]
name = "infrahub-test-client"
version = "0.3.0"
edition = "2021"

[dependencies]
infrahub = { path = ".." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
